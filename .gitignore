# Build artifacts
bin/
dist/
*.exe
*.test
*.out

# Coverage
coverage.out
coverage.html
*.cover

# Local config / secrets
.env
.env.local
*.local

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp
*~

# Local SQLite DBs (tests)
*.db
*.db-journal
*.sqlite
*.sqlite3

# tmp
tmp/

# Bench output (current.txt is a transient artefact of `make bench-check`;
# baseline.txt IS committed)
bench/current.txt

# nox scan artefacts. findings.json (at repo root) IS committed (the
# security baseline); other formats regenerate on every scan.
.nox/out/

# Built praxis binary at repo root (artefact of `make build` /
# `goreleaser`). Anchored to the root so cmd/praxis/ source dir is
# not matched.
/praxis
